import SwiftUI

enum CanvasGridSizing {
    static let cellSize = CGSize(width: 80, height: 24)
    static let minBodyRows = 1
    static let minBodyCols = 1
}

enum CanvasChartSizing {
    static let defaultSize = CGSize(width: 360, height: 240)
    static let minSize = CGSize(width: 200, height: 140)
}
